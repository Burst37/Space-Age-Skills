# reference/

Vendored reference material — **not skills**. Nothing in this directory is auto-loaded by Claude; it's here for you to consult or fork from manually.

## screenshot-to-code/

Upstream: [abi/screenshot-to-code](https://github.com/abi/screenshot-to-code) (MIT licensed — see `screenshot-to-code/LICENSE`). A full Next/Vite + FastAPI application that converts screenshots, mockups, and Figma designs into code (HTML/Tailwind/React/Vue/Bootstrap/Ionic).

Vendored as a snapshot on 2026-08-19 from a user-supplied zip. Unrelated to the Seedance skills added alongside it in the same PR — it landed in the same upload batch but isn't a skill, so it's kept here rather than under `skills/`.
