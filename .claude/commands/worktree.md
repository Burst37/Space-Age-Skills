Load and follow the using-git-worktrees skill from `.claude/skills/obra-superpowers/using-git-worktrees/SKILL.md`.

Detect existing isolation first — do not create a new worktree if one exists. Verify clean baseline before starting. Each worktree is a single independent task.

Arguments: $ARGUMENTS (optional — branch or task name)