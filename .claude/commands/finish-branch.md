Load and follow the finishing-a-development-branch skill from `.claude/skills/obra-superpowers/finishing-a-development-branch/SKILL.md`.

Verify tests pass → detect environment → present 4 options (merge, PR, deploy, abandon) → execute chosen option → cleanup. Do not merge without presenting options first.

Arguments: $ARGUMENTS (optional — branch name or context)