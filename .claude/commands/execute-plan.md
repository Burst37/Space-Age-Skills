Load and follow the executing-plans skill from `.claude/skills/obra-superpowers/executing-plans/SKILL.md`.

Sequential task execution from a written plan. Mark each task complete as soon as it is done. Stop immediately on any blocker — do not skip ahead. Use finishing-a-development-branch when all tasks are done.

Arguments: $ARGUMENTS (optional — the plan to execute)