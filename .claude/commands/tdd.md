Load and follow the test-driven-development skill from `.claude/skills/obra-superpowers/test-driven-development/SKILL.md`.

Strict RED-GREEN-REFACTOR cycle. No production code without a failing test first. Write the test, confirm it fails, write minimum code to pass, refactor, repeat.

Arguments: $ARGUMENTS (optional — what feature/function to TDD)