# Secrets — never commit. .env.example is the template; .env is yours.
.env
.env.*
!.env.example

node_modules/
.next/
dist/
build/

# Gauntlet run artifacts: screenshots, scroll recordings, keyframes, state
runs/
workspace/
gauntlet.project.json

*.log
.DS_Store
